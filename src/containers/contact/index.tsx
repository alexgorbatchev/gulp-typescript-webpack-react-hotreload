import * as React from 'react';
import * as ReactDOM from 'react-dom';
import Contact from '../../components/contact';

export default class ContactContainer extends React.Component<any, any> {
  render() {
    return <Contact />
  }
}
