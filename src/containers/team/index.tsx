import * as React from 'react';
import * as ReactDOM from 'react-dom';
import Team from '../../components/team';

export default class TeamContainer extends React.Component<any, any> {
  render() {
    return <Team />
  }
}
